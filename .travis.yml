language: elixir
sudo: false

jobs:
  include:
    - stage: test
      elixir: 1.7.4
      otp_release: 19.3
      after_success: mix coveralls.travis

    - stage: code quality
      elixir: 1.7.4
      otp_release: 19.3
      script:
        - mix format --check-formatted
        - mix credo --strict
